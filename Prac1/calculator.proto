syntax = "proto3";

message Int {
    int32 value = 1;
}

message String {
    string value = 1;
}

message Map {
        map<string, int32> value = 1;
    }

service Calculator {
    rpc countingWords(String) returns (Int) {}
    rpc wordCount(String) returns (String) {}
}